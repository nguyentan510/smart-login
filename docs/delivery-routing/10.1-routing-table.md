# 10.1 — The routing table

Spec: [`../delivery-routing.md`](../delivery-routing.md) (D1). Status:
[`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Make the transport a configured fact instead of a property of the
destination string — while changing no site's behaviour.

This sub-phase ships alone and must be invisible. Nothing routes anywhere new
yet; the only difference is that routing has become something a setting can
answer.

## Files

- `includes/OTP/Transports/class-transport-router.php`
- `includes/class-field-registry.php`
- `tests/delivery/run-routing-tests.php`

## Steps

1. **Two settings**, tab `delivery`, section `otp` for now (10.6 moves them):

   | Path | Type | Choices | Default |
   | --- | --- | --- | --- |
   | `delivery.route_phone` | select | `sms` | `sms` |
   | `delivery.route_email` | select | `email` | `email` |

   One choice each at this point. 10.3 adds `automation` to both. A select with a
   single option is deliberate: it renders, it is saved, and it proves the path
   exists before anything depends on it.

2. **Split `transport_for()` in two.** The channel derivation is legitimate and
   stays; the transport decision moves to the table:

   ```php
   public function channel_for( string $destination ): string {
       return ( false !== strpos( $destination, '@' ) ) ? 'email' : 'phone';
   }

   public function transport_for( string $destination ): string {
       $channel = $this->channel_for( $destination );
       $routed  = (string) Settings::get( 'delivery.route_' . $channel, '' );

       return $this->get( $routed ) ? $routed : self::FALLBACK[ $channel ];
   }
   ```

   `FALLBACK` is `array( 'phone' => 'sms', 'email' => 'email' )` — the values the
   defaults already produce. It is not redundant with the defaults: a stored
   option can name a transport that a filter used to register and no longer does,
   and falling back beats resolving to nothing.

3. **Do not touch `send()`.** It already prefers `$ctx['transport']` when present
   (`class-transport-router.php:62`), which is what keeps the admin tester and
   any caller with an explicit choice working unchanged.

4. **Rule 5 goes green** — the choices and the resolvable transports are now the
   same set, and stay so because both read the registry.

## Acceptance

Rule 5 green, rule 1 still green. Behaviour identical: a phone destination
resolves to `sms` and an email destination to `email`, asserted both through
`transport_for()` directly and through a full `OtpService::issue()` on each
channel.

`php tests/run-all.php` — **all** required suites must be at their previous
counts, not merely passing. A changed count here means the routing change was not
invisible and must be understood before it lands.

## Not in scope

The `automation` choice, and any UI reorganisation. Both need something to route
to, and there is nothing yet.

---

## Outcome

Rules 1 and 5 green. `Delivery routing: 17 passed, 2 failed, 5 pending` — the two
failures are rules 2 and 3 and belong to 10.2.

Every required suite finished at the count it had at `933c17e`:

```text
  Regression 301 · Identity core 175 · contract 39 · fitness 28
  Templates 77 · Admin screens 61 · Account surface 7 · Abuse boundary 28
```

phpcs at the 17/15 baseline. The alignment did not move: `delivery.route_phone`
is shorter than `otp.max_per_destination_hour`, which already sets the column in
this array — the failure mode 9.2 hit does not apply here.

### The rule this sub-phase was supposed to turn green nearly went red instead

Rule 1's second assertion read `transport_for()`'s body and required the literal
`delivery.route_`. Putting the two paths in a `ROUTES` class constant — the right
shape, since the fallback belongs beside the setting it backs — moved the literal
out of the method body and would have failed a rule for an improvement.

Rewritten as behaviour: register a third transport, point a route at it, assert
the answer changes. That version is indifferent to how the key is spelled and to
where it lives, and it asserts the property the rule is actually about. A
structural rule that a better implementation breaks is a rule pinned to one
spelling — 9.3 had to rewrite rule 3c for the same reason.

### Two rules pulled in opposite directions

The abuse suite's rule 8 forbids `Settings::get()` on a key `FieldRegistry` does
not declare, matched by a literal quote after the paren. Building the path inline
as `Settings::get( 'delivery.route_' . $channel, '' )` would have captured
`delivery.route_` as an undeclared key and turned a **required** suite red.

Reading by variable is what the constant map does anyway, and rule 8 skips
variable first arguments by design — the field renderer reads by dynamic path for
the same reason. Two rules from different phases constrained the implementation
from both sides and the shape that satisfies both is the better one.

### `channel_for()` speaks the vocabulary that already existed

It returns `phone` and `email`, which are `PhoneChannel::ID` and
`MailChannel::ID` verbatim. Checked rather than assumed: 10.5 will call this
method to count OTPs by channel, and a router with its own private spelling would
have made that a translation layer instead of a lookup.

### What is asserted that the suites staying green does not prove

The defaults reproducing the old `'@'` answer is the entire no-migration
argument, so it is asserted directly — a phone resolves to `sms`, an email to
`email` — rather than inferred from nothing having broken. The fallback is
asserted too: a stored route naming a transport nothing registers resolves to the
built-in, not to nothing, because a filter that adds a transport can be removed
at any time.
