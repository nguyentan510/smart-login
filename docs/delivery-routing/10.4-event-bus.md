# 10.4 — The event bus

Spec: [`../delivery-routing.md`](../delivery-routing.md) (D2 bus role). Status:
[`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Push the events the plugin already records to the same automation
endpoint, so an external system can react to registrations, logins, resets and
abuse trips without polling anything.

The plugin has nineteen event constants and no way to tell anyone about them
(`includes/Security/class-audit-log.php:19-39`). Once 10.3 has a signer and a
sender, carrying them costs almost nothing — and it is the answer to "cảnh báo
đăng nhập" that does not require the plugin to grow a mail composer.

## Files

- `includes/OTP/Transports/class-event-bus.php` — new
- `includes/Security/class-audit-log.php` — one dispatch point
- `includes/class-field-registry.php`
- `tests/delivery/run-routing-tests.php`

## Steps

1. **One dispatch point, not fifteen.** Every event already funnels through
   `AuditLog::record()`. Hooking the bus there means a new event constant is
   automatically busable and cannot be forgotten — the same argument
   `FieldRegistry` makes for settings. Do **not** scatter `do_action` calls
   through the handlers.

2. **Setting** `automation.events`, a checkbox list built from the `AuditLog`
   constants, tab `delivery`, section `automation`. Default: empty. The bus is
   off until an administrator picks events; a plugin that starts POSTing a site's
   login stream somewhere on upgrade would be indefensible.

   The choices are generated from the constants, not typed out, so a constant
   added later appears without a second edit. Assert that the option can hold
   only known constants, or a stale stored value keeps firing an event nobody
   recognises.

3. **Never the code.** The bus envelope omits `code` entirely — not blank, not
   masked, absent. Destinations use the same masking `AuditLog` already applies
   (`RateLimiter::mask_identity()`), because the bus inherits the audit log's
   privacy posture rather than inventing a looser one.

4. **Non-blocking.** `'blocking' => false`. The consequence is stated, not hidden:
   the site cannot know whether a bus event arrived. That is the correct trade —
   `WebhookTransport::MAX_TIMEOUT`'s comment explains what synchronous outbound
   calls do to a PHP-FPM pool (`class-webhook-transport.php:18-25`), and doubling
   them on every login is the version of this feature that takes a site down.

   The admin tester sends blocking, since a test the administrator cannot read
   the result of is not a test.

5. **Its own breaker,** id `automation_bus`. A non-blocking call still costs a
   connection, so a dead endpoint on a busy site is still worth stopping. It must
   never be consulted on the OTP path — rule 6 exists for this.

6. **Its own audit event.** `AUTOMATION_BUS_FAILED`, and it must be in
   `NEVER_SAMPLED` — a sampled failure record on a fire-and-forget channel is the
   worst of both, since it is the only evidence there is. Guard against recursion:
   a bus failure recording an audit entry must not dispatch a bus event.

## Acceptance

Rule 6 green: a bus endpoint returning 500 leaves `OtpService::issue()` returning
an array and the OTP row intact. Rule 4 still green — bus sends go through the
same signer.

An event not ticked produces no request at all. A ticked event produces one
request whose body verifies and contains no `code` key, asserted by
`array_key_exists`, not by checking the value is empty.

Recursion: forcing a bus failure produces exactly one audit record and exactly
one outbound attempt.

`php tests/run-all.php`. The abuse-boundary suite is the one to watch — the bus
hooks the same `AuditLog::record()` that 9.1 and 9.3 depend on.

## Not in scope

Retry and delivery guarantees. Fire-and-forget is the design, and a site needing
at-least-once semantics should point the endpoint at a queue it controls. Written
here rather than left to be discovered.

Event filtering by user, role or intent. One checklist, whole events.

---

## Outcome

Green, and the suite has no PENDING lines left.

```text
Delivery routing: 45 passed, 0 failed, 0 pending
```

Every required suite at its previous count; phpcs at the 17/15 baseline; the
whole integration chain re-run, because the bus hooks `AuditLog::record()` and
every gate exercises that:

```text
SMART_LOGIN_AUTH_INTEGRATION_OK
SMART_LOGIN_GOOGLE_STAGING_SMOKE_OK
SMART_LOGIN_PROVIDER_LINKING_OK
SMART_LOGIN_ZALO_STAGING_SMOKE_OK
SMART_LOGIN_ABUSE_GATE_OK
SMART_LOGIN_DELIVERY_GATE_OK
```

### The sender had to be extracted, and rule 4 is what said so

The bus needs to post a signed envelope, and so does the transport. Writing a
second `wp_remote_request()` call would have meant a second place to forget the
signature, and the guard rail caught the attempt the moment the bus landed:

```text
Rule 4 — automation sends only through the signer
  FAIL     exactly one file puts an automation request on the wire
           → includes/OTP/Transports/class-automation-endpoint.php:75
```

`AutomationEndpoint` now owns the wire — url, secret, headers, timeout, sign,
send — and both roles differ in one argument, `$blocking`. The rule is stronger
than it was in 10.3: the one allowlisted file's *only* job is sending signed
envelopes, so "signed" is not a discipline anyone has to remember.

This is the second time a structural rule has been right about a shape while
being wrong about the file. In 10.3 it named the signer as the sender; here it
named the transport. Both times the fix was to move the code, not the rule.

### The bus shares the audit log's hourly cap, deliberately

9.9 stopped the audit log amplifying the attack it records: past
`security.audit_max_per_event_hour` an event writes one summary row instead of
thousands. An outbound HTTP call costs strictly more than an `INSERT`, so a bus
that fired below that cap would rebuild the same defect one layer up and aim it
at somebody else's server.

Dispatch therefore sits after `may_write()` — which also means **turning the
audit log off turns the bus off**. That is a real coupling between two settings
that read as unrelated. It is in the field's help text, not only here.

`may_write()` has a side effect: it increments the hourly transient. So the
dispatch had to go inside the existing call rather than beside a second one, or
every event would have counted twice against its own cap.

### The breaker is weaker than the brief implied

`'blocking' => false` means WP_Http returns before the endpoint answers, so the
bus learns about immediate failures — DNS, connection refused, a malformed URL —
and nothing else. A 500 from a reachable endpoint is invisible to it.

The breaker therefore protects against an endpoint that is *gone*, not one that
is *broken*. That is still worth having, since a dead host costs a connection
attempt on every audited event, but the brief's framing suggested more coverage
than fire-and-forget can give. Stated here rather than left for someone to
discover from a breaker that never opens.

### Two things the tests caught that the brief did not name

**A checkbox list needs an empty hidden input.** Without it, "none ticked" is
absent from `$_POST`, and `sanitize()` reads absence as "not on this tab" and
leaves the stored value alone — so the last subscribed event could never be
switched off.

**The regression suite's round-trip generator needed teaching.** It posts a
sample value for every field on a tab and demands it back, and it already carried
the reason why: *a field with its own sanitiser needs a sample that sanitiser
accepts, otherwise the round-trip measures the rule rather than the plumbing.*
`https_url` and `audit_events` are both such rules. It failed on
`automation.events` first — an invented event name is exactly what the sanitiser
exists to reject.
