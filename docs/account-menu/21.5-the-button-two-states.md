# 21.5 — The button, two states

Spec: [`../account-menu.md`](../account-menu.md), decisions 1, 7, 8.
Status: [`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Turn rules 10, 11 and 12 green. `[smart_login_button]` renders as a
button, knows who is looking at it, and works with the script blocked.

This is the sub-phase that fixes finding 1: the trigger built for people who
cannot edit templates has never had a stylesheet.

## Files

- `includes/Frontend/class-shortcodes.php` — `render_button()` rewritten
- `includes/Frontend/class-assets.php` — register the stylesheet, and add the
  shortcode to `maybe_enqueue()`'s tag list
- `assets/css/smart-login-button.css` — new
- `templates/account-button.php` — new
- `tests/integration/run-account-menu-gate.php` — its first rules

## Steps

1. `render_button()` branches on `is_user_logged_in()` and renders through
   `TemplateLoader`, not `sprintf()`. A template is what a theme can override,
   and every other surface in this plugin is already overridable.
2. **Signed out** — an `<a>` whose `href` is the real sign-in page, carrying
   `data-smart-login`, exactly as today (`class-shortcodes.php:63-69`). The
   `href` is not touched by anything, for the reason 19.4 wrote down.
   Default label `Đăng nhập`.
3. **Signed in** — `<details class="sl-account">` with a `<summary>` carrying the
   label from `account_menu.label_source`, and the `AccountMenu` list inside.
   No `data-smart-login` anywhere in this branch: rule 10.
4. `auto` resolves to `UserManager::META_PHONE` when set, else `display_name`.
   Never the email: spec decision 12.
5. Markup carries `.sl-account-btn`, and **no `sl-btn`** — rule 11, spec
   decision 7. The stylesheet makes it `inline-flex`, so it is the size of its
   contents wherever it is put.
6. `smart-login-button.css` declares `smart-login-tokens` as its dependency and
   restates no colour. It is registered and enqueued whenever the shortcode
   renders, and `smart_login_button` joins the tag list in
   `Assets::maybe_enqueue()` (`class-assets.php:43`) so the stylesheet is in the
   head rather than the footer — the flash of unstyled form that comment already
   describes, applied to a button in a header where it is more visible.
7. The logout entry gets a separator above it, in CSS. It is a design constant,
   not a setting, and it is how the reference reads.
8. No JavaScript in this sub-phase. `<details>` opens and closes on its own.

## Acceptance

- Rules 10, 11, 12 green
- Rendered signed-out markup contains `data-smart-login` and no `<details>`;
  rendered signed-in markup contains `<details>` and no `data-smart-login`
- The signed-in `<summary>` shows the phone for a member who has one and the
  display name for one who does not, asserted with both fixtures
- A member with a synthetic email never sees that address in the button
- **Integration gate:** a page hosting only `[smart_login_button]` has the button
  stylesheet in its enqueued styles, and does not have `smart-login.css`. This is
  finding 1's rule and it is measured in a real WordPress, because an enqueue is
  the class of defect a fixture reports as fine
- **Integration gate:** the token stylesheet is enqueued ahead of the button's
- **Integration gate, inherited from 21.3:** the logout entry's URL carries a
  real nonce and differs between two users. The pure suite cannot show this —
  `wp_logout_url()` is stubbed to a fixed string there, so asserting it would
  measure the stub
- Every other suite at its previous count

## Not in scope

Outside-click, `Escape`, `aria-expanded`, and the mobile sheet — 21.6. The
dropdown must be demonstrably useful with no script before any script is added,
or decision 8 is a claim nobody checked.

---

## Outcome

```text
Account menu: 31 passed, 4 failed, 0 pending  ->  35 passed, 0 failed, 0 pending
  Every rule from 21.0 is green.

Integration gate, against the real site, 6 checks, 0 failed:
  OK  a page with only [smart_login_button] enqueues the button stylesheet
  OK  and does not drag in the sign-in form stylesheet
  OK  the token stylesheet is printed, and before the button
  OK  the logout URL carries a nonce
  OK  the logout URL differs once the visitor is somebody
  OK  AccountMenu hands out that generated URL, not a stored one

Templates: 109 -> 115  (two new templates, each bringing its own assertions)
Every other suite at its previous count:
  Regression 342 · Identity core 175 · Identity contract 50 · Identity fitness 48
  Admin screens 147 · Account surface 10 · Abuse boundary 31 · Delivery routing 72
  Mail templates 56 · Sign-in card 15 · Account card 41 · Rendered surface 25
  Sign-in anywhere 60
  Coding standards: 21 ERRORS AND 17 WARNINGS IN 15 FILES (unchanged)
```

### The suite was promoted here, not in 21.7

Every rule went green when the button landed. The working agreement promotes a
`spec` suite **the moment** that happens, and 15.4 found that rule being ignored
for four phases. Waiting two sub-phases to honour it would have been the same
mistake with a shorter fuse, so `kind` is `required` from this commit and 21.7's
step 7 is already done.

### 21.3's deferral is discharged here

The pure suite could not show that the logout URL is generated rather than
stored — `wp_logout_url()` is stubbed to a fixed string there. Against a real
WordPress it carries `_wpnonce=` and changes when the visitor does, and
`AccountMenu` is handing out that value rather than a copy. Written down as
owed in 21.3, paid in 21.5, rather than left to be noticed by its absence.

### Finding 1, measured rather than argued

`Assets::maybe_enqueue()` gates on `is_singular()` and a shortcode in
`post_content`, so nothing short of a real page and a real query proves it
fires. The gate creates one, runs the enqueue, reads `wp_styles()->queue`, and
deletes the page again.

The second check is the one that makes the split worth having: the page carries
the button stylesheet and **not** `smart-login.css`. Ordering is asserted over
the *printed* document rather than over registration, because WordPress resolves
a dependency at print time and registration order proves nothing about output.

### The template suite refused two templates with no fixture

`every template has a fixture or a written-down exclusion` went red immediately:
`account-button` and `login-button` had neither, and a template with no fixture
is a template no test ever executes. The rule's own history is why it exists —
the WooCommerce account template went six phases unexecuted behind an exclusion,
and that is where the account-surface drift accumulated.

Two fixtures rather than one, because the shortcode renders two different
templates and this file holds one entry per template. Templates went 109 → 115
as a result; that is a suite growing with its subject, not a count drifting.
