# 21.2 — One glyph vocabulary

Spec: [`../account-menu.md`](../account-menu.md), decision 6.
Status: [`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Turn rules 2 and 3 green. One place decides what a UI glyph looks like,
and an icon that is not in it cannot be stored, let alone rendered.

## Files

- `includes/Frontend/class-icon-set.php` — new
- `includes/Frontend/class-account-form.php` — `sections_meta()` reads from it
- `templates/onboarding.php` — **found by 21.0's rule 2**, draws its own glyph
- `templates/partials/password-field.php` — likewise
- `templates/partials/account/card-head.php` — unchanged, and that is the test

## Steps

1. `IconSet::get( string $name ): string` returns inline SVG at 18×18, stroked,
   `currentColor` — the geometry `sections_meta()` already established
   (`class-account-form.php:93-96`), lifted rather than re-decided.
2. `IconSet::names(): array` returns name => Vietnamese label, and it is what the
   settings `<select>` is built from. One list, so a name the picker offers is a
   name the renderer has by construction.
3. Ten names to start: `user`, `lock`, `map-pin`, `shield`, `box`, `file-text`,
   `calendar`, `pill`, `heart`, `log-out`. The first four are the four already
   inlined in `sections_meta()` and their paths are moved verbatim — this
   sub-phase does not redraw an icon.
4. `IconSet::FALLBACK` is `user`. `get()` with an unknown name returns the
   fallback's markup, and never the input.
5. `sections_meta()` keeps its four entries, their labels and their order
   (spec deferral). Only the `$svg` closure goes, replaced by an `IconSet` call.
6. **Two call sites the spec did not count.** 21.0's rule 2 found inline SVG in
   `templates/onboarding.php` and `templates/partials/password-field.php` as
   well. Both are UI glyphs, neither is a brand mark, so neither qualifies for
   the exemption and both convert here. Whatever names they need are added to
   the set in step 3 rather than drawn inline "just this once".
6. **Provider marks are not touched.** `ProviderMark::icon_svg()` returns a
   trademark in its owner's colours (`class-provider-mark.php:63`); it is named
   in rule 2 as the exemption rather than left to be matched by a pattern.

## Acceptance

- Rules 2 and 3 green
- `card-head.php` is **byte-identical** before and after, and the account-card
  suite's rendered output is unchanged — the refactor is invisible or it is
  wrong
- `IconSet::get( 'no-such-icon' )` returns the fallback markup; the string
  `no-such-icon` appears nowhere in the output
- Every name in `IconSet::names()` resolves to non-empty markup, asserted by
  iterating the list rather than by spot-checking
- Every other suite at its previous count

## Not in scope

Redrawing any of the four existing icons, or letting a site add an icon. A filter
over the set is a later question and it has a real answer to find first — an
added icon is user-supplied SVG, and rule 3 exists to keep that off the page.

---

## Outcome

```text
Account menu: 7 passed, 8 failed, 9 pending  ->  11 passed, 6 failed, 7 pending
  Rules 1, 2 and 3 green

Byte-identity, asserted directly rather than inferred:
  user   byte-identical: YES
  check  byte-identical: YES
  eye    byte-identical: YES
  sections_meta profile icon === the old string: YES
  unknown name leaks input: NO

templates/partials/account/card-head.php: untouched, 0 lines changed

Every other suite at its previous count:
  Regression 342 · Identity core 175 · Identity contract 50 · Identity fitness 48
  Templates 109 · Admin screens 147 · Account surface 10 · Abuse boundary 31
  Delivery routing 72 · Mail templates 56 · Sign-in card 15 · Account card 41
  Rendered surface 25 · Sign-in anywhere 60
  Coding standards: 21 ERRORS AND 17 WARNINGS IN 15 FILES (unchanged)
```

### The two extra call sites are not the same kind of glyph, and the brief assumed they were

21.0 found inline SVG in `onboarding.php` and `password-field.php` and this brief
said to convert them. Reading them showed why that is not a formality: the four
in `sections_meta()` are 18×18 with `fill="none"` and a 1.8 stroke; the
onboarding checkmark is **28×28 and solid**; the password eye is **20×20 and
solid**.

Flattening all six into one wrapper would have changed how three shipped
surfaces look, inside a sub-phase whose neighbour had just established that a
diff claiming "nothing looks different" may not also change a value. The eye is
the sharper case — `.sl-field--password` reserves `padding-right: 46px` for that
button, and `smart-login.css` already documents that number as control geometry
rather than rhythm. Shrinking the glyph would leave the reservation describing
nothing.

So `IconSet` holds **per-icon geometry**, and the attribute strings are
reproduced exactly as each call site had them, attribute order included.

### `names()` and `get()` are two different questions

The split that made the above work:

- `names()` — the **pickable vocabulary**, ten entries, one visual style. What a
  settings row may choose and what rule 3 iterates.
- `get()` — markup for **any** glyph, pickable or not.

`check` and `eye` are in the second and not the first. They are fixed parts of
one control each and nobody will ever choose them from a menu; offering them
would let a 28px solid checkmark appear in a row of 18px outlines.

This is the same shape as 21.1's `--sl-dlg-*` decision — a thing that never
crosses the boundary stays local to its side — with one difference worth naming:
a glyph's *markup* still benefits from one home, so these live in `IconSet` while
staying out of the vocabulary. That let rule 2 stay a blanket "no inline `<svg>`
outside `IconSet` and the provider mark" rather than growing an allowlist, and an
allowlist is where future exceptions would have hidden.

### The POT catalogue caught the ten new labels

`Identity fitness` went 48 → 47 the moment `IconSet::names()` landed: eight new
translatable strings in the tree and none in the catalogue. The failure named its
own fix — `php bin/build-pot.php` — and the rebuilt catalogue is 698 strings from
134 files.

Worth recording because it is the second guard rail in two sub-phases to catch a
consequence the brief had not listed. 21.1's was the account-card suite; this one
is the POT check.
