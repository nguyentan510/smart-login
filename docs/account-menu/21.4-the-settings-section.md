# 21.4 — The settings section

Spec: [`../account-menu.md`](../account-menu.md), decisions 3, 6, 12.
Status: [`../refactor-plan.md`](../refactor-plan.md).

**Goal.** An administrator can add a row to the middle of the menu — icon, text,
link — and choose what the signed-in button calls the member. No new machinery:
this is finding 6's repeater with a third column.

## Files

- `includes/class-field-registry.php` — one section, two field rows
- `includes/class-settings.php` — one sanitiser
- `includes/Admin/class-field-renderer.php` — one control
- `includes/Frontend/class-account-menu.php` — step 2 of its assembly wakes up

## Steps

1. `FieldRegistry::sections()` gains `account_menu => 'Menu tài khoản'`, on the
   `profile` tab. The tab already holds "Trường hồ sơ" and "Địa chỉ 2 cấp"; the
   account menu is the same family.
2. `account_menu.items` — `type => 'menu_items'`, `sanitize => 'menu_items'`,
   `default => array()`. The row lives in `profile_fields()` alongside the rest
   of the tab, because membership and rendering read the same row
   (`class-field-registry.php:13-16`) and a key placed anywhere else is the
   defect that class exists to prevent.
3. `account_menu.label_source` — `type => 'select'`, choices `auto` / `phone` /
   `name`, default `auto`. Three choices, and **no email**: spec decision 12.
4. `FieldRenderer::menu_items()`, modelled on `headers()`
   (`class-field-renderer.php:458-502`): a `widefat` table, `max( 5, count + 1 )`
   rows, always one spare, no JavaScript. Columns: icon `<select>` built from
   `IconSet::names()`, text `<input type="text">`, link `<input type="url">`.
5. `Settings::sanitize_menu_items()`, modelled on `sanitize_headers()`
   (`class-settings.php:706-750`):
   - a row with an empty label **or** an empty URL is dropped, silently, the way
     a blank header row is
   - `key` is derived with `sanitize_title()` from the label when absent, then
     de-duplicated with a numeric suffix — an administrator does not type a key,
     but decision 4 still needs every entry to have a stable one
   - `key` is refused if it collides with `account` or `logout`; the pinned ends
     own those names
   - `icon` is passed through `IconSet`, so an unknown name becomes the fallback
     and never survives as a string
   - `url` through `esc_url_raw()`
6. `AccountMenu` now reads the stored list for the middle of its assembly. It
   remains the only reader of the option — rule 9.

## Acceptance

- Saving three rows and reloading the tab shows three rows and one spare
- Saving a row with a label and no URL stores nothing, and does not error
- Saving two rows both labelled `Đơn hàng` yields two distinct keys
- A row whose label is `Đăng xuất` does **not** take the pinned tail's key
- Posting `icon=<script>` stores the fallback name; the string appears nowhere in
  the rendered admin screen or the rendered menu
- Clearing every row returns `AccountMenu::items()` to the two pinned entries —
  rule 7 still green, which is the regression this step could most easily cause
- The admin-screens suite passes with the new controls drawn
- Every other suite at its previous count

## Not in scope

Reordering by drag, a page picker, and the live preview beside the table — all
three are spec deferrals with their reasons written there. Row order in the table
is menu order.

---

## Outcome

```text
Account menu: 22 passed, 5 failed, 1 pending  ->  31 passed, 4 failed, 0 pending
  Rule 9 green; seven new save-path assertions green
  Remaining red: the four button rules, which are 21.5

Every other suite at its previous count:
  Regression 342 · Identity core 175 · Identity contract 50 · Identity fitness 48
  Templates 109 · Admin screens 147 · Account surface 10 · Abuse boundary 31
  Delivery routing 72 · Mail templates 56 · Sign-in card 15 · Account card 41
  Rendered surface 25 · Sign-in anywhere 60
  Coding standards: 21 ERRORS AND 17 WARNINGS IN 15 FILES (unchanged)
  POT rebuilt: 708 strings from 135 files
```

### The regression suite caught the sanitiser before it ever ran

`saving "profile" keeps every value it posted` went red as soon as
`account_menu.items` joined the tab. The round-trip test generates a sample per
field, had no case for `menu_items`, and fed the sanitiser a string — which it
correctly turned into an empty array, producing a mismatch.

The comment directly above that switch already said what to do: *"A field with
its own sanitiser needs a sample that sanitiser accepts, otherwise the
round-trip measures the rule rather than the plumbing."* The sample carries the
**derived** key, `don-hang`, rather than inventing one — a sample that posted its
own key would round-trip only because the assertion was comparing two copies of
one guess.

### `sanitize_title()` was not stubbed, and the fixture would have lied

Chasing that failure found something worse: **no suite could reach this
sanitiser at all.** `sanitize_title()` and `remove_accents()` have never been
stubbed, so the first row with a real label would have fatalled in test-land. It
had not shown up because the bad sample made the function return before it got
there.

Production was never affected — both functions are WordPress's. But the gap
matters for a function whose entire job is refusing bad input, and the stub had
to be **accent-folding** rather than a token one: this plugin's labels are
Vietnamese, WordPress folds `Đơn hàng` to `don-hang`, and a stub that skipped
that would produce different keys in tests than in production. That is a fixture
telling a comfortable lie about the one input the feature actually receives.
Verified against the real behaviour: `sanitize_title( 'Đơn hàng' ) = don-hang`.

### A row is never dropped for an underivable key

The brief said a label that yields no slug means the row is dropped. Writing the
stub made the consequence concrete: a label in a script `remove_accents()` cannot
transliterate reduces to nothing, and the row disappears with no message.

Losing a menu item because of the alphabet its label is written in is not a
defensible refusal, so the key falls back to the row's position — `item-3` —
and the row survives. Asserted with a label of `★★★`.

### The five refusals, each asserted

Driven through `Settings::sanitize()` rather than the private method, because
what matters is what pressing Save produces:

| Input | Result |
| --- | --- |
| label, no URL | dropped silently, like a blank header row |
| `Đơn hàng` twice | two distinct keys |
| a row labelled `Đăng xuất` | does **not** take the pinned tail's key |
| `icon=<script>alert(1)</script>` | stored as the fallback; the input does not survive |
| every row cleared | `items()` returns the two pinned entries |
