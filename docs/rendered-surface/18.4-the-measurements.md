# 18.4 — The measurements nobody took

Spec: [`../rendered-surface.md`](../rendered-surface.md), decision 5. Status:
[`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Take the readings three phases promised, and write them down as
numbers. Close 16.3's and 17.3's outstanding items, or say they are still open.

Last, because it needs everything above it.

## Files

- `docs/sign-in-card/16.3-card-geometry.md` — its two unrun checks
- `docs/account-card/17.3-one-action-shape.md` — its two unrun checks
- This file's own Outcome

No production files. If a measurement finds a defect, it gets its own sub-phase
rather than being fixed in the commit that found it.

## Steps

1. Render the account composite and the sign-in card with 18.1's renderer.
2. Take the readings the README specifies, at 375, 480 and 1400.
3. Keyboard-only pass through one account card: tab order, focus visibility, the
   two `<details>`, the expanding contact editor.
4. JavaScript off: the unlink form and the password disclosure.
5. Write every number into the Outcome. A reading that fails becomes a finding
   with a sub-phase, not a fix in this commit.

## Acceptance

- Every reading recorded as a number or a described observation, never as a tick
- 16.3's and 17.3's briefs updated to point at the results, including any that
  are still not taken and why
- No production file changed by this sub-phase

## Not in scope

Fixing what the measurements find. That is the next phase's material, and
conflating the two is how a measurement stops being trustworthy.
