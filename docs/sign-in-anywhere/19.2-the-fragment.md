# 19.2 — The fragment

Spec: [`../sign-in-anywhere.md`](../sign-in-anywhere.md), decisions 1 and 2.
Status: [`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Turn rule 7 green. One endpoint that renders any step of the flow as
HTML, from the same templates the shortcode uses, with a fresh nonce every time.

## Files

- `includes/Frontend/class-fragment-renderer.php` — new
- `includes/Frontend/class-rest-controller.php` — `step` route
- `includes/Frontend/class-shortcodes.php` — `render_flow()` gains a render
  context; the shortcode path is unchanged

## Steps

1. `GET smart-login/v1/step?step=…&page=…&redirect_to=…` returns
   `{ step, html, title }`. `POST` to the same route runs a step through
   `FlowEngine` and returns the **next** fragment, or `{ redirect }` when the
   flow is finished.
2. **The renderer is given the host page explicitly.** `Flow::url()` computes
   step links with `remove_query_arg()` against the current request
   (`class-flow.php:125`), which inside a REST request is the API URL. A render
   context carries the host page URL, and `Flow::url()` reads it when set. Same
   for `redirect_to`, which `form-auth.php:30` reads from `$_GET`.
   `page` is passed through `wp_validate_redirect()` — it is request input that
   ends up in an `href`.
3. `Assets::enqueue()` is not called on this path. A REST request has no
   `wp_enqueue_scripts`, and calling it there is a silent no-op that looks like
   it works.
4. Notices ride in the fragment, not in a separate field: `partials/notices` is
   already inside every template and a second channel would be a second source
   of truth.
5. The captcha widget needs its provider script present *before* the fragment
   arrives. `Assets::register_assets()` registers `CAPTCHA_HANDLE` only when
   `Captcha::script_url()` is non-empty (`class-assets.php:80-84`); the launcher
   loads it alongside the main script on first open, so the fragment never
   contains a widget with no script.
6. Rendering `STEP_OTP` requires the `smart_login_flow` cookie; without it the
   endpoint returns the identify step rather than an OTP form with nothing behind
   it. `otp_args()` already handles the reload case (`class-shortcodes.php:238-270`)
   and is reused rather than reimplemented.

## Acceptance

- Rule 7 green
- A fragment fetched for a given host page contains that page's URL in
  `redirect_to` and in every step link; `/wp-json/` appears nowhere in it
- An `off-site` value for `page` is rejected, asserted directly
- Two fragments fetched a second apart carry **different** stamps and both verify
- The identify fragment fetched over REST is byte-identical to the shortcode's,
  once the host-page base is accounted for — the single-template contract Phase
  8.2 established
- Every other suite at its previous count

## Not in scope

The dialog, the trigger contract, the redirect policy. This sub-phase can be
exercised entirely with `curl`.

---

## Outcome

```text
Rule 1 — every public step is reachable over REST      (green)
Rule 7 — the fragment does not link into the API       (green)

Sign-in anywhere: 21 passed, 3 failed, 7 pending   (was 15 / 5 / 7)
```

Every required suite PASS.

### Rule 7 was a structural check, and structural was not enough

As landed in 19.0 it asserted that `Flow` had gained a `set_base()`. That says
nothing about whether the templates read it — a renderer could have the method
and every fragment could still point at `/wp-json/`.

It now renders one. The stubbed `remove_query_arg()` answers `/my-account/` for
"the current request", so a fragment that ignored its base says so out loud, and
the rule asserts four things about real markup: it renders, it carries no
`/wp-json/`, `redirect_to` is the host page, and `/my-account/` appears nowhere.

That found the second half of finding 8 in passing: `templates/form-auth.php`
read `$_GET['redirect_to']` inline. In a REST render that is the API request's
query string, so the visitor would have been returned to the endpoint. It now
asks `Flow::redirect_to()`, which validates either source.

### The nonce argument turned into a route decision

`/step` is the only route in the namespace that does not require the `wp_rest`
nonce, and writing down why is the point.

`wp_localize_script()` puts its nonce in the page's HTML. A launcher on every
page of a cached site therefore holds a **cached** nonce — the same defect that
stops the dialog carrying its form markup, one layer up. Requiring it would have
made the dialog fail on exactly the sites the fetch design exists to serve.

What replaces it is stronger, not weaker: GET renders a public form and changes
nothing, and POST carries `smart_login_nonce`, the signed timestamp and the
honeypot — all minted when the fragment was fetched seconds earlier, and checked
by `RequestGuard::verify()` inside `FlowEngine`. The route is guarded by a
fresher nonce than the one it declines.

### `welcome_url()` was carrying the same bug

Not in the plan, found while wiring the base through: `FlowEngine::welcome_url()`
computed the current URL the same way `Flow::url()` did. A registration finished
through a fragment would have redirected the new member to
`/wp-json/smart-login/v1/step?smartlogin_welcome=1`. Fixed here rather than left
for 19.5, because it is the same defect and not a different one.

### The string catalogue moved

Six new strings — the dialog's per-step titles. `languages/smart-login.pot`
rebuilt: **681 strings from 129 files**. The fitness suite caught the stale
catalogue in the same run.
