# 11.4 — The mail screen

Spec: [`../mail-templates.md`](../mail-templates.md). Status:
[`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Somewhere to edit the messages, grouped the way an administrator
thinks about them rather than the way the registry stores them.

Sequenced last, for the reason 10.6 was: a screen can only present a model that
exists.

## Files

- `includes/class-field-registry.php` — the tab and its sections
- `includes/Admin/Screens/class-settings-screen.php` — per-message chrome
- `includes/Admin/class-field-renderer.php` — the token list beside a body
- `assets/css/admin.css`
- `tests/identity/run-admin-tests.php`

## Steps

1. **A second-level tab under Gửi mã**, slug `delivery-mail`, label
   *Nội dung email*. Same constraint as 10.6: it has its own Save button, so it
   is a real tab slug, and it hangs off `delivery` through `tab_parents()`.

   Not a new top-level tab. The strip is at six and the reason 10.6 built a
   second level was to stop it growing.

2. **One section per registry group**, in registry order: *Mặc định* (the shared
   fallback pair), *Mã xác thực* (the four intents), *Cảnh báo quản trị* (the
   two from 11.3), *Giao diện* (the layout settings from 11.2).

3. **Each message states when it fires.** The row's `when` text renders above
   its two fields. An administrator editing "Đặt lại mật khẩu" should not have to
   infer from the label whether it covers a forgotten password, a changed one, or
   both.

4. **The token list sits beside the body it belongs to**, from that row's
   declared set — not the global table currently rendered once under the SMS
   section. That table stays where it is; this is a different list for a
   different reason.

5. **Empty means inherited, and says so.** A blank override renders with a
   placeholder showing the text that will actually be sent, so an empty box reads
   as "using the default" rather than as "this email has no subject".

## Acceptance

Every generated field is drawn by exactly the tab that claims it — the same walk
10.6 added, now covering rows that no human typed.

Saving the mail screen leaves `sms.*`, `email.*` and `automation.*`
byte-identical, and saving the email channel screen leaves the overrides
untouched.

A message with no override shows the inherited text as a placeholder and stores
nothing; typing into it stores an override; clearing it restores inheritance.

The delivery gate renders this screen alongside the other four.

## Not in scope

Preview and send-a-test-of-this-message. Both are worth having and neither is
needed to edit a template; they are a follow-up, recorded here so the absence is
a decision.

---

## Outcome

Green, and Phase 11 closes.

```text
Admin screens: 101 → 110 passed
Mail templates:      35 passed, 0 failed, 0 pending
```

Every other required suite unchanged; phpcs at the 17/15 baseline; whole
integration chain OK.

Five screens under Gửi mã now: Chính sách mã, Kênh SMS, Kênh Email, Nội dung
email, Automation.

### The empty box was the whole problem

Eight override fields that all ship blank look like eight emails with no
subject. An administrator faced with that pastes the default into every one of
them — and inheritance, the thing 11.1 built the three-level resolution for,
is dead the first afternoon somebody tidies the screen.

So an empty box shows what it will actually send, as its placeholder, resolved
through the same `MailRegistry::resolve()` the transport calls. Asserted on the
exact string, because "there is a placeholder attribute" would pass on an empty
one.

### Token scoping is asserted in both directions

Each body carries its own collapsed token list. The assertion that matters is
not that `{{ceiling}}` appears — it is that it appears **fewer than six times**:
present for the budget alert, absent beside the four OTP bodies. Counting is what
distinguishes "scoped" from "rendered somewhere".

### The gate got weaker again, the same way

10.6 found `run-delivery-gate.php` passing while rendering one screen of four.
This sub-phase added a fifth, and the gate's hard-coded list of four would have
gone on reporting OK while ignoring the newest one — the screen most likely to
break, since it is the only one whose fields are generated.

Second time in two phases that a green gate covered less than it had. The list
is a single array now and the labels no longer say "four", but the general
lesson stands: a gate that enumerates is a gate that goes stale, and nothing in
the suite can tell you that it has.

### Deviations from this brief

**The tab was created in 11.1**, not here, so the generated fields landed in
their final home rather than being moved twice.

**`when` renders as the subject field's help, not as a heading above the pair.**
A per-message header inside a `form-table` costs real chrome for a line of text
that is already adjacent to both fields and above the body. Recorded rather than
quietly dropped.

**`email.is_html` moved onto this screen too.** The brief left it on the channel
tab, but it decides whether the layout applies, and the layout settings are
here — an administrator turning HTML on wants the three appearance fields in
front of them, not one tab away.
