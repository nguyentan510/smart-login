# 11.3 — The admin alerts join the registry

Spec: [`../mail-templates.md`](../mail-templates.md) (D4). Status:
[`../refactor-plan.md`](../refactor-plan.md).

**Goal.** Stop the plugin sending two messages nobody can reword, redirect or
switch off.

## The two

```php
wp_mail( $to, sprintf( __( '[%s] Đã tạm dừng gửi mã xác thực', … ) ), … );
```
`includes/Security/class-rate-limiter.php:267` — the site budget halted.

```php
wp_mail( $to, sprintf( __( '[%s] Kênh gửi mã đang lỗi liên tục', … ) ), … );
```
`includes/OTP/Transports/class-circuit-breaker.php:163` — a transport opened.

Both go to `get_option( 'admin_email' )`, both compose their own subject and
body inline, and neither is reachable from any screen. They are also the two
messages most likely to arrive at three in the morning during an incident.

## Files

- `includes/Mail/class-mail-registry.php`
- `includes/Mail/class-mailer.php` — new
- `includes/Security/class-rate-limiter.php`
- `includes/OTP/Transports/class-circuit-breaker.php`
- `includes/class-field-registry.php`
- `tests/mail/run-template-tests.php`

## Steps

1. **A sender for messages that are not OTPs.** `MailTransport` exists to
   deliver a code and is answerable to the router and the breaker; an operational
   alert is neither. `Mailer::send( string $message_id, string $to, array $ctx )`
   resolves through the registry, applies the layout, and calls `wp_mail()`.

   That makes it the second and last caller, which is what rule 1 asserts.

2. **Two registry rows**, group `admin`, with the current wording as their
   defaults and their own token sets — `{{ceiling}}`, `{{window}}`,
   `{{halt_minutes}}` for the budget; `{{transport}}`, `{{cooldown}}` for the
   breaker. Rule 3 checks those defaults against those sets, which is how a typo
   in a default is caught before an incident is the thing that reveals it.

3. **An off switch per message**, default on. The reason to allow off is 10.4:
   both events already reach an automation endpoint through the bus, so a site
   with that configured is receiving each alert twice and can currently silence
   neither.

4. **Recipient stays `admin_email`.** Not configurable here. A recipient field
   invites a distribution list, which invites per-message recipients, which is
   the feature D6 declines for the login alert and the same argument applies.

5. **The breaker keeps its once-per-transition discipline.** `announce()` is
   called at the moment the count crosses so a burst produces one mail rather
   than one each; moving the composition out must not move that.

## Acceptance

Rule 1 green — no `wp_mail()` in `includes/` outside `MailTransport` and
`Mailer`.

Both alerts still fire, with their current wording byte-identical while nobody
has overridden them. Turning one off stops that mail and leaves the audit record
and the bus event untouched — the alert is a notification, not the record.

A burst of concurrent breaker failures still produces exactly one mail.

## Not in scope

New alerts. This sub-phase moves two messages and adds none.

---

## Outcome

Rule 1 green. `Mail templates: 18 → 28 passed, 0 failed, 1 pending` — the one
pending is rule 5, which belongs to 11.2.

Every required suite at its previous count; phpcs back to the 17/15 baseline
after `phpcbf` realigned two arrays.

### The rule's own allowlist was the design

Rule 1 said "no `wp_mail()` outside `MailTransport`", and the fix adds a second
entry rather than satisfying the first. That is not the rule being weakened: it
was written in 11.0 knowing 11.3 would add exactly one more sender, and the
value is that the list is now two and visibly finite. A third entry has to be
argued for in the test file.

`MailTransport` could have taken the alerts instead, and that would have been
worse: it is chosen by the router, guarded by the circuit breaker and answerable
for delivery, none of which is true of an operational alert — and one of which
is circular, since the breaker is what sends one of them.

### What the switch is actually for

Both alerts were unreachable: no wording, no recipient, no off. The wording and
the recipient are now registry concerns, but the switch is the part that answers
the real complaint, and 10.4 is why it is defensible — `OTP_BUDGET_HALTED` and
`TRANSPORT_BREAKER_OPEN` already reach an automation endpoint through the event
bus, so a site with that configured was receiving each alert twice and could
silence neither.

Asserted alongside it: switching the mail off leaves the **audit record**
written. The log is the evidence read afterwards; the mail is a notification, and
conflating them would mean an operator who quietened their inbox also blinded
their history.

### Tokens are scoped both ways

`ADMIN_TOKENS` is deliberately not `OTP_TOKENS` plus extras. An alert has no
code, no destination and no expiry, so inheriting them would put four
always-empty tokens in front of whoever is editing the message.

The reverse needed care too: `available_tokens()` with no id is what the SMS
section calls, and adding `{{ceiling}}` to the shared table would have leaked
operational tokens into that list. It now filters to the OTP set, so the SMS help
is byte-identical to what it was.

### A guard against the quiet failure

One assertion is worth naming: the sent alert must contain no `{{`. An
unexpanded token is the silent-empty-string defect with the braces left in, and
it is exactly what a hand-built map missing one key produces — which is what
moving from `sprintf()` to token rendering could have introduced.
